#include<stdio.h>
#include<conio.h>
int main()
{
int num;
printf("\n enter the number=");
scanf("%d",&num);
if (num>0)

printf("\n %d is an positive  number",num);

else
printf("\n %d is an negative number.",num);

return 0;
}